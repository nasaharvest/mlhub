name: mlhub
channels:
  - conda-forge
dependencies:
  - python=3.7
  - pip
  - jupyter
  - requests
  - xarray
  - rasterio  # to read tif and jp2 files
  - black  # formatting
  - mypy  # type checking
  - pip:
    - -e .
